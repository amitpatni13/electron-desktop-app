import { Injectable } from '@angular/core';
// import { Firebase } from '@ionic-native/firebase';
@Injectable()
export class LogIntoFirebase {

    // constructor(private firebase: Firebase ) {}

  // log the events as per the button clicked by user
 logIntoFirebaseEvent(button: string, params: string[]) {
    // const paramObj = {};
    // for (let prmIndx = 0 ; prmIndx < params.length ; prmIndx++){
    //   paramObj[prmIndx] = params[prmIndx];
    //   console.log('paramObj' , paramObj);
    // }
    // this.firebase.logEvent(button, paramObj).then(firebaseResponse => {
    //   console.log('firebaseResponse :', firebaseResponse);
    // });
  }
}
